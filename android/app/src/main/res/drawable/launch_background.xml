<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fundo branco limpo para a inicialização do sistema -->
    <item android:drawable="@android:color/white" />

    <!-- Removido o ícone daqui para evitar que a appimg apareça no carregamento do Android -->
</layer-list>
